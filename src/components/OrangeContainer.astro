<div class="wrapper">
  <div class="container">
    <slot />
  </div>
</div>

<style>
  .wrapper {
    background-color: var(--secondary-color);
    padding: 0 20px;
  }

  .container {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 20px;
    margin: 0 auto;
    padding: 20px 0;
    max-width: var(--container-max-width);
  }
</style>
