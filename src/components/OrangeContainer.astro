<div class="wrapper">
  <div class="container">
    <slot />
  </div>
</div>

<style>
  .wrapper {
    align-self: stretch;
    background-color: var(--color-secondary);
    padding: 0 20px;
  }

  .container {
    display: flex;
    flex-direction: column;
    margin: 0 auto;
    padding: 20px 0;
    max-width: var(--container-max-width);
  }
</style>
